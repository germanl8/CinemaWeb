'use strict'

window.addEventListener("load", function(){
 /*
    var id_movie = 0;
        if (localStorage.getItem("id_movie") == null){ //corrobora si no hay nada ingresado en el local storage, o sea si es el primer registro.
            localStorage.setItem("id_movie",id_movie); // si efectivamente no hay nada entonces guarda el primer valor en el localstorage, con key id_user y el valor 0
        }

       

        
*/




});